{
	"name": "onon-fs2",
	"private": true,
	"scripts": {
		"dev:back": "bun run --filter backend dev",
		"test": "bun test",
		"lint": "bun run --filter backend lint && bun run --filter frontend lint",
		"lint:fix": "bun run --filter backend lint:fix && bun run --filter frontend lint:fix",
		"format": "bun run --filter backend format && bun run --filter frontend format",
		"check": "bun run --filter frontend check"
	},
	"workspaces": ["apps/*"]
}

{
	"name": "onionfs",
	"private": true,
	"scripts": {
		"dev:back": "bun run --filter storeagent dev",
		"test": "bun test",
		"lint": "bun run --filter storeagent lint && bun run --filter frontend lint",
		"lint:fix": "bun run --filter storeagent lint:fix && bun run --filter frontend lint:fix",
		"format": "bun run --filter storeagent format && bun run --filter frontend format",
		"check": "bun run --filter frontend check"
	},
	"workspaces": ["apps/*", "packages/*"]
}
